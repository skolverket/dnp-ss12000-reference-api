apiVersion: v1
kind: Service
metadata:
  name: provisioning-reference-api-service-test-0
  labels:
    app: pra-test-1
spec:
  type: LoadBalancer
  ports:
    - name: expose
      protocol: TCP
      port: 8888
      targetPort: 8888
    - name: ingest
      protocol: TCP
      port: 8889
      targetPort: 8889
  selector:
    app: provisioning-reference-api


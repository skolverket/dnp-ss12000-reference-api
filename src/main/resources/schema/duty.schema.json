{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "/schemas/duty",
  "title": "Duty",
  "description": "A duty.",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "format": "uuid"
    },
    "person": {
      "$ref": "/schemas/objectReference"
    },
    "dutyAt": {
      "$ref": "/schemas/objectReference"
    },
    "dutyRole": {
      "type": "string"
    },
    "startDate": {
      "type": "string",
      "format": "date"
    },
    "endDate": {
      "type": "string",
      "format": "date"
    }
  },
  "required": [
    "id",
    "person",
    "dutyAt",
    "dutyRole",
    "startDate"
  ]
}
